# Optional OpenRouter key for the LLM service. If unset, the LLM is fully
# disabled (it returns enabled=false and is never called for anything secret).
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openai/gpt-4o-mini
